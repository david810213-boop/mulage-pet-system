# 慕沐村 Mulage pet — 疑難排解與環境設定指南

> 這份文件整理專案從 H2 換成 MySQL、串接 ngrok + LINE 之後，實際遇過的所有問題與排除方式。
> 目的是讓你（或未來接手的人）遇到類似狀況時能直接查表處理，不用重新從頭摸索。

---

## 一、目前環境架構總覽

| 項目 | 內容 |
|---|---|
| 資料庫 | MySQL 8.0，跑在 Docker 容器 `pet-mysql`，對外 port **3307**（對應容器內部 3306） |
| 應用程式 | Spring Boot，本機 port **8081** |
| 對外暴露 | ngrok（`ngrok http 8081`），免費版網址每次重啟會換新的 |
| LINE 登入 | LINE Login channel（LIFF 用），channel-id 設定在 `application.yml` |
| LINE 推播 | LINE Messaging API channel（另一個 channel，跟登入用的不同），token 透過環境變數 `LINE_MESSAGING_CHANNEL_ACCESS_TOKEN` 帶入 |

### 每次開發前的固定啟動順序

```bash
docker start pet-mysql          # 1. 確認資料庫容器在跑
mvn spring-boot:run             # 2. 啟動應用程式（固定用同一種方式啟動，見下方說明）
.\ngrok http 8081                # 3.（若需要外部測試 LIFF）另開一個終端機跑 ngrok
```

若 ngrok 重啟過，記得回 LINE Developers Console 的 LIFF 設定更新新網址（`https://新網址/liff/index.html`）。

---

## 二、啟動失敗類問題

### 2.1 `Connection refused` / `Communications link failure`

**原因**：MySQL 容器沒有在跑。

**排查**：
```bash
docker ps
```
看不到 `pet-mysql` 或狀態不是 `Up`，就跑：
```bash
docker start pet-mysql
```
若連 `docker start` 都失敗、顯示連不上 Docker API，代表 **Docker Desktop 本身沒有啟動**，先手動開啟 Docker Desktop，等鯨魚圖示變成綠色 Running 再重試。

### 2.2 `Port 8081 was already in use`

**原因**：已經有另一個 process 佔用 8081，通常是**忘記關掉的舊實例**，尤其是 VS Code 的執行按鈕跟終端機指令交替使用時特別容易發生（見第四節）。

**排查**：
```bash
netstat -ano | findstr :8081
```
找到最後那欄的 PID，強制關閉：
```bash
taskkill /PID <該PID> /F
```

### 2.3 `Public Key Retrieval is not allowed`

**原因**：MySQL 8 的 `caching_sha2_password` 認證機制，在沒開 SSL 的連線下預設拒絕自動取得金鑰。

**已修復**：`application.yml` 的連線字串已加上 `allowPublicKeyRetrieval=true`。若又遇到這個錯誤，檢查連線字串是否被還原成沒有這個參數。

> ⚠️ 這個參數組合只適合本機開發環境。正式部署到有網路暴露風險的環境時，應改用 `useSSL=true` 或 `mysql_native_password` 驗證方式。

### 2.4 Docker 啟動時跳出「Lingering processes detected」

**原因**：Docker Desktop 背景程序沒有乾淨關閉（常發生在重開機後）。

**處理**：直接點視窗上的 **Stop processes**，讓它清掉殘留程序、重新啟動即可，不會影響任何專案資料。

---

## 三、資料庫 Schema 相關問題（`ddl-auto=update` 的已知限制）

專案用 Hibernate `ddl-auto=update` 管理 schema（無 Flyway）。**它很會「新增」東西，但「調整既有欄位限制」不可靠**，這是這個專案踩過最多次的坑，務必記住這個模式。

### 3.1 新增 NOT NULL 欄位到既有表 → 失敗

**症狀**：`NULL not allowed for column "XXX"` 或 `Field 'xxx' doesn't have a default value`

**原因**：既有資料列沒有值可以填入新的 NOT NULL 欄位。

**正確寫法**（已在 `GroomingItem.bookable` 套用）：
```java
@Column(nullable = false, columnDefinition = "boolean default false")
@Builder.Default
private boolean bookable = false;
```
用 `columnDefinition` 給資料庫層級預設值，讓既有列自動填入，新增才會成功。**新增任何 NOT NULL 欄位前，先想到這件事。**

### 3.2 放寬既有欄位的 NOT NULL 限制 → 不會自動生效

**症狀**：Java 端已經把 `nullable = false` 拿掉了，但實際寫入資料時仍然報 `Column 'xxx' cannot be null`。

**原因**：Hibernate 對「放寬限制」的 schema 同步不可靠，不像新增欄位、改型別那樣穩定。

**處理**：手動下 SQL：
```bash
docker exec pet-mysql mysql -uroot -proot petdb -e "ALTER TABLE 表名 MODIFY COLUMN 欄位名 型別 NULL;"
```
例如這次遇到的：
```bash
docker exec pet-mysql mysql -uroot -proot petdb -e "ALTER TABLE performance_records MODIFY COLUMN appointment_id BIGINT NULL;"
```

### 3.3 欄位名稱剛好是 MySQL 保留字 → 建表失敗

**症狀**：`SQL syntax error ... near 'xxx date not null...'`

**這次遇到的案例**：`year_month` 是 MySQL 保留字（用在 `INTERVAL ... YEAR_MONTH` 語法），已改名為 `perf_month`。

**排查方式**：如果新增欄位名稱剛好撞到 SQL 保留字，直接改名字最省事，不用跳脫符號。

### 3.4 改欄位型別（例如字串改成外鍵）→ 舊欄位變孤兒，且可能仍是 NOT NULL

**這次遇到的案例**：`walk_in_order_items.operator`（原本是 `varchar NOT NULL`）改成 `operator_staff_id`（外鍵）後：
- Hibernate 新增了新欄位，但**沒有刪除**舊的 `operator` 欄位
- 因為程式碼不再往舊欄位塞值，但它還是 `NOT NULL`，導致每次 `INSERT` 都失敗：`Field 'operator' doesn't have a default value`

**處理**：手動刪除孤兒欄位：
```bash
docker exec pet-mysql mysql -uroot -proot petdb -e "ALTER TABLE walk_in_order_items DROP COLUMN operator;"
```

**⚠️ 重要教訓**：只要把某個欄位從 entity 裡「改名/改型別/整個換掉」，**不要以為舊欄位留著沒事**——如果它原本是 NOT NULL，會直接讓新增資料的功能整個壞掉。改欄位當下就該順手排入待辦，找時間刪掉舊欄位，不要拖到炸了才處理。

### 3.5 通用排查口訣

看到 **`cannot be null`**、**`doesn't have a default value`**、**`Data too long for column`** 這類跟資料庫欄位限制有關的錯誤，先反射性懷疑是 `ddl-auto` 沒把限制同步到位，直接考慮手動下 `ALTER TABLE`，不用浪費時間懷疑程式邏輯本身。

---

## 四、環境變數沒生效（VS Code vs 終端機）

**症狀**：明明用 `set LINE_MESSAGING_CHANNEL_ACCESS_TOKEN=xxx` 設定過，終端機 `echo` 也確認有值，但應用程式的 log 還是顯示 `尚未設定 LINE_MESSAGING_CHANNEL_ACCESS_TOKEN`。

**根本原因**：`set` 指令設定的環境變數，**只對「設定之後、在同一個終端機視窗裡啟動」的程式有效**。如果應用程式是被 **VS Code 的執行按鈕**啟動的（可以從啟動 log 開頭那行 `cmd /C ""...java.exe" @...\Temp\cp_xxxx.argfile ...` 認出來），它是 VS Code 自己開的另一個 process，完全不會繼承你在外部終端機設定的變數。

**正確做法**：改設**永久系統環境變數**（一次設定，任何啟動方式都吃得到）：

1. 開始鍵 → 搜尋 `Edit the system environment variables` → 打開它
2. **Environment Variables...** → 上半部 **User variables** 區塊 → **New...**
3. Variable name：`LINE_MESSAGING_CHANNEL_ACCESS_TOKEN`
4. Variable value：貼上 token（**務必確認 `=` 後面沒有多餘空格**，`set X= value` 跟 `set X=value` 是不一樣的，前面那個空格會變成值的一部分）
5. 一路確定關閉視窗
6. **完全關閉 VS Code（所有視窗），用工作管理員確認沒有殘留的 `java.exe` process，再重新打開**——環境變數是作業系統層級的，已經開著的程式不會自動抓到新設定的值

**建議**：之後固定用同一種方式啟動（要嘛都用終端機 `mvn spring-boot:run`，要嘛都用 VS Code 按鈕），不要交替使用，避免出現兩個 process 互搶 8081 port 的狀況（就是第 2.2 節那個問題的常見成因）。

---

## 五、LINE 推播沒送出的排查表

按下「確認預約」後，看終端機 log 裡 `[LINE推播]` 開頭的那一行，對照下表：

| Log 內容 | 原因 | 處理方式 |
|---|---|---|
| `已發送給 U....` | 成功送出 | 若對方仍收不到，檢查對方是否已加官方帳號好友 |
| `尚未設定 LINE_MESSAGING_CHANNEL_ACCESS_TOKEN，訊息未送出` | 環境變數沒帶到這次啟動的 process | 見第四節，改設永久系統環境變數並重開啟動程式 |
| `略過發送：此會員沒有綁定 LINE` | 該會員是帳密註冊，不是 LINE 登入 | 正常行為，不是 bug |
| `發送失敗，不影響主要操作。原因：...` | Token 失效、或對方沒加官方帳號好友 | 到 LINE Developers Console 確認 token 還有效；確認 QR code 加好友步驟有沒有做 |
| 完全沒有 `[LINE推播]` 這行 | 請求根本沒送到後端 | 確認畫面上真的按到「確認預約」按鈕、該筆預約狀態原本是「待確認」（黃色標籤），不是已經確認過的單 |

**正式上線前提醒**：目前設定的是**測試用**官方帳號（「推推寵物美容」）的 token。正式上線時：
1. 要換成店家真正的官方帳號 token
2. 系統要部署到正式伺服器（不是你自己的電腦），屆時環境變數要設定在那台伺服器上，不是這台 Windows 的系統變數裡

---

## 六、常用指令速查

```bash
# 資料庫容器
docker ps                                              # 看容器狀態
docker start pet-mysql                                 # 啟動容器
docker exec pet-mysql mysql -uroot -proot petdb -e "SQL指令"   # 對資料庫下 SQL

# 找出佔用某個 port 的程式並關閉
netstat -ano | findstr :8081
taskkill /PID <PID> /F

# 啟動應用程式（固定用這個方式，避免跟 VS Code 按鈕衝突）
cd C:\Projects\Pet_System
mvn spring-boot:run

# 對外暴露（另開一個終端機視窗）
cd C:\Projects\Pet_System
.\ngrok http 8081
```

---

## 七、如果以後要換成正式部署環境（不再是你自己電腦 + Docker）

以下是屆時要重新確認的項目，先列在這裡提醒自己：

- [ ] MySQL 改用正式的雲端資料庫服務（不是本機 Docker 容器），`application.yml` 的 `DB_HOST`/`DB_PORT`/`DB_USERNAME`/`DB_PASSWORD` 環境變數要指向新位置
- [ ] `useSSL=false&allowPublicKeyRetrieval=true` 這組本機開發用參數要重新評估，正式環境建議改用 SSL
- [ ] `LINE_MESSAGING_CHANNEL_ACCESS_TOKEN` 換成店家正式官方帳號的 token
- [ ] 不會再有 ngrok 這一層，LIFF Endpoint URL 要改成正式網域
- [ ] 之前手動下過的 `ALTER TABLE` 語句（放寬 `appointment_id`、刪除孤兒欄位 `operator` 等）如果正式環境是全新建立，`ddl-auto=update` 建新表時就會直接照 Java 目前的 entity 定義建對，不會重複踩到同樣的坑；但如果是從這個環境把資料搬過去，要記得這些手動調整過的欄位需要一併搬過去
