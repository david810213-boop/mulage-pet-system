# 慕沐村 Mulage pet — 八項需求修改說明

> Schema 由 Hibernate `ddl-auto` 管理（無 Flyway）。新欄位／新表會自動建立；
> 既有資料的回填由 `config/DataBackfillRunner.java` 於開機時執行一次（等同一份輕量 migration，冪等，可重複執行；確認無誤後可移除）。
>
> 關於 `ddl-auto` 的注意事項：新增的 enum 欄位一律以 `@Enumerated(EnumType.STRING)` 儲存；
> 新欄位若一開始就標 `nullable=false` 會讓既有 row 違反約束，故新增欄位皆給預設值或允許 null。

---

## 前置結論：錢包餘額如何儲存 → 扣款鎖 `Wallet` 列

餘額存在 `Wallet.balance` 欄位（`Wallet` 與 `User` 一對一）；`WalletTransaction` 只是帳本歷程，每筆帶 `balanceAfter` 快照，**餘額不是靠加總算出來的**。

原本 `WalletService.deduct()` 是「讀餘額→比較→寫回」但**沒有鎖**，並發下會超扣。已改為以悲觀鎖（`SELECT ... FOR UPDATE`）重讀錢包列後扣款：`WalletRepository.lockByUserId()` + `deduct()`。

---

## 需求 1：線上儲值僅限轉帳

**做法**：改為「申請 → 店家核帳 → 入帳」兩段式，餘額不會在顧客送出當下變動。

- 新增 `TopUpRequest`（狀態 `PENDING/CONFIRMED/REJECTED`）、`TopUpStatus`、`TopUpRequestRepository`。
- `TopUpService`：`submit()` 建立 PENDING；`confirm()` 呼叫**既有** `WalletService.deposit()` 真正加值（沿用贈點／升卡規則，不另寫一套）；`reject()` 駁回。
- API：顧客 `POST /api/topup`、`GET /api/topup/my`；店家 `GET /api/admin/topup/pending`、`POST /api/admin/topup/{id}/confirm`、`POST /api/admin/topup/{id}/reject`。
- 前端 `wallet.html`：新增「線上儲值（限轉帳）」卡片，填金額＋後五碼＋轉帳時間送出，並顯示申請狀態。轉帳帳號為預留值，請店家自行替換。

## 需求 2：長短毛由店家定義

- `Pet.coatType` 預設改為 `UNDEFINED`；`PetRequest` 移除 `coatType`（顧客不再傳）；`PetService.addPet()` 一律寫入 `UNDEFINED`。
- 新增 `PetService.setCoatType()` 與店家 API `PUT /api/admin/pets/{petId}/coat-type`（`AdminPetController`）。
- 前端 `add-pet.html`：移除毛長選項與相關驗證／送出欄位。
- 後台 Thymeleaf 新增寵物表單保留毛長選擇（店家可用），`PetMvcController.submit()` 以 `@RequestParam coatType` 接收並於建立後 `setCoatType()`。

## 需求 3：同時段最多五隻 + 店家確認最後時間

- **併發控管**：新增 `SlotCapacity`（每個 `(日期,開始時間)` 一列，`booked`／`capacity=5`）＋ `SlotCapacityService`。
  - `ensureSlot()`（`REQUIRES_NEW`）確保計數列存在，靠 unique 約束擋重複；
  - `reserve()`／`release()` 在預約主交易內對該列加**悲觀鎖**後 `+1`／`-1`，同時段並發請求序列化，杜絕超賣（無法對「還不存在的預約列」加鎖，故用計數表）。
- `AppointmentService.book()`：以 `reserve()`（上限 5）取代原本「一時段一單」的重疊擋單；新單狀態設為 `PENDING_CONFIRM`。
- `AppointmentService.cancel()`：`release()` 釋放名額。
- `getAvailableSlots()`：回傳每時段 `booked/capacity/remaining`（前端可顯示「剩 N 位」）。
- **店家確認**：`Appointment` 新增 `confirmedTime`；`confirm()` 由店家敲定最後時間並轉 `CONFIRMED`；API `POST /api/admin/appointments/{id}/confirm`。

## 需求 4：預約端只顯示大美容／小美容／精緻洗／定製洗

- `GroomingItem` 新增 `bookable`（預設 false）；`GroomingItemRepository.findByBookableTrueAndIsDeletedFalse()`。
- `/api/grooming-items` 改為只回 `bookable=true`（LIFF 預約頁用）；新增 `/api/grooming-items/all` 給後台／現場開單。
- `GroomingItemRequest`/`GroomingServiceImpl` 支援設定 `bookable`；`GroomingItemResponse` 帶出 `bookable`。
- `DataBackfillRunner` 會把名稱含「大美容/小美容/精緻洗/定製洗」的既有項目自動標為可預約。

## 需求 5 & 6：現場開單 + 經手人（預設 X）+ 存交易紀錄 + 後續補填算積分

- 新增 `WalkInOrder`（現場單＝交易紀錄）＋ `WalkInOrderItem`（每項 `operator` 預設 `"X"`＝未填寫，並快照名稱/價格/積分/分類）。
  - 不動既有 `Transaction`（其 `appointment_id` 為 NOT NULL、單一經手人），避免破壞線上預約結帳流程。
- `WalkInOrderService`：`create()` 開單；`pendingOperatorItems()` 撈 `operator='X'` 待補；`fillOperator()` 補填；`operatorPointsReport()` 依經手人分組加總（排除 `X`）。
- API（`WalkInOrderController`）：`POST /api/admin/walk-in-orders`、`GET`（全部）、`GET /pending-operator`、`PUT /items/{itemId}/operator`、`GET /points-report`。

## 需求 7：預約備注雙可見性

- `Appointment` 新增 `internalNote`（店家內部：耳朵加強去油、造型需求…）與 `memberNote`（會員可見）。
- **防洩漏關鍵**：顧客端 `AppointmentResponse` 只帶 `memberNote`，**絕不含** `internalNote`；後台專用 `AppointmentAdminResponse` 才含兩者。
- API：`PUT /api/admin/appointments/{id}/notes`（設定）、`GET /api/admin/appointments`（後台清單含內部備注）。

## 需求 8：後台備注會員特殊資訊

- `User` 新增 `adminNote`（後台專用，`UserResponse` 不含此欄位）。
- `UserService.getAdminNote()/setAdminNote()`；API `GET/PUT /api/admin/members/{username}/note`（`AdminMemberController`）。

---

## 新增／修改檔案清單

**新增（Java）**
enums：`TopUpStatus`
model：`TopUpRequest`、`SlotCapacity`、`WalkInOrder`、`WalkInOrderItem`
repository：`TopUpRequestRepository`、`SlotCapacityRepository`、`WalkInOrderRepository`、`WalkInOrderItemRepository`
dto：`TopUpRequestCreate`、`TopUpRequestResponse`、`TopUpReviewRequest`、`SetCoatTypeRequest`、`ConfirmAppointmentRequest`、`AppointmentNoteRequest`、`AppointmentAdminResponse`、`WalkInOrderCreateRequest`、`WalkInOrderResponse`、`FillOperatorRequest`、`OperatorPointsResponse`、`MemberNoteRequest`
service：`TopUpService`、`SlotCapacityService`、`WalkInOrderService`
controller：`TopUpController`、`AdminPetController`、`AppointmentAdminController`、`WalkInOrderController`、`AdminMemberController`
config：`DataBackfillRunner`

**修改（Java）**
`Pet`、`Appointment`、`User`、`GroomingItem`（欄位）、`PetRequest`、`PetResponse`(不變)、`AppointmentResponse`、`GroomingItemResponse`、`GroomingItemRequest`、`TimeSlotResponse`、`WalletRepository`、`GroomingItemRepository`、`WalletService`、`AppointmentService`、`PetService`、`GroomingService`(介面)、`GroomingServiceImpl`、`UserService`、`GroomingItemController`、`PetMvcController`

**修改（前端）**
`static/liff/add-pet.html`（移除毛長）、`static/liff/wallet.html`（轉帳儲值申請）

---

## 附註：移除孤兒檔案 `enums/TransactionType.java`

專案裡原有一個內容為空的 `TransactionType.java`，對應的是先前規劃階段（尚未檢視實際碼庫前）提出的通用草案
`TransactionType { TOPUP, CONSUMPTION }`。實際比對真實架構後，這兩種語意已分別由更精確的設計取代，
不需要也沒有任何地方引用這個 enum，因此已刪除：

- 「TOPUP」→ 由獨立實體 `TopUpRequest` + `TopUpStatus` 取代（需要記帳號後五碼、核帳人等欄位，單一 enum 不夠用）
- 「CONSUMPTION」→ 由既有 `Transaction`（線上預約結帳，搭配 `PaymentMethod`）與新增的 `WalkInOrder`（現場開單，搭配 `PerformanceCategory`）分別取代

全專案已確認無任何 import 引用此檔案，刪除不影響其他程式碼。

---

## 附註：資料庫由 H2 改為 MySQL

因專案即將接真實顧客資料，且已導入悲觀鎖（時段名額、錢包扣款）等併發控管邏輯，
H2 內嵌檔案資料庫不適合作為長期環境（單一 process 存取、`ALTER TABLE` 行為與正式環境的
MySQL 不同、無法代表真實部署的鎖行為），故改為 MySQL。異動如下：

- `pom.xml`：移除 `h2` / `spring-boot-h2console` 依賴，改為 `com.mysql:mysql-connector-j`。
- `application.yml`：datasource 改指向 MySQL，帳密改用環境變數（`DB_HOST`/`DB_PORT`/`DB_NAME`/
  `DB_USERNAME`/`DB_PASSWORD`），未設定時預設對應本機 Docker 容器（`localhost:3306`／`root`/`root`／`petdb`）。
- `WebConfig.java`：移除攔截器排除清單中的 `/h2-console/**`（已不存在對應功能）。
- `ddl-auto` 維持 `update`：因這次是全新空白 schema，`CREATE TABLE` 會把所有新增欄位（含
  `bookable`）直接建好，不會觸發先前在 H2 上遇到的「既有表加 NOT NULL 欄位」問題。

**本機啟動 MySQL（建議用 Docker）**：

```bash
docker run --name pet-mysql \
  -e MYSQL_ROOT_PASSWORD=root \
  -e MYSQL_DATABASE=petdb \
  -p 3306:3306 \
  -d mysql:8.0
```

之後每次開發：`docker start pet-mysql`。

> 未攜帶既有 H2 測試資料（依你的選擇，直接空白 schema 重新開始）。若之後仍需要那批舊測試資料，
> 可另外用 `mysqldump`／匯入腳本處理，屆時再說。

---

## 部署提醒

1. 本專案未在此環境編譯（沙箱無法連 Maven Central）。請在你本機 `mvn clean package` 跑一次確認。
2. 開機會執行 `DataBackfillRunner`：重建時段計數、標記可預約項目。確認資料無誤後可刪除該檔。
3. `wallet.html` 的轉帳帳號為預留值，請替換為實際店家帳號。
4. 若日後要多開 instance，時段名額與扣款皆已走 DB 悲觀鎖，可安全水平擴充。
5. 積分整合：現場單 `operator` 為字串。若要接既有 `PerformanceService`（以 `User` 計績效），可在 `fillOperator()` 補填時，把 operator 對應到員工 `User` 後呼叫 `performanceService.addRecord(...)`。
