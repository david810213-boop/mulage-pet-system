# 慕沐村 Mulage Pet 寵物美容管理系統 — 專案總覽

**用途**：這份文件是給新對話串開頭貼上用的，濃縮專案全部背景，避免重新從頭解釋。 **更新日期**：2026-08-22（本次更新：貓狗套餐化全面完成、LIFF 獨立入口全部補齊）

---

## 一、技術棧

- **後端**：Spring Boot 4.0.6、Java 17（本機曾出現 Java 21/25 環境，注意版本一致性）  
- **模板**：Thymeleaf（伺服器端渲染，`th:` 語法）  
- **資料庫**：MySQL，Spring Data JPA \+ Hibernate 7.2.12，`ddl-auto: update`  
- **建置**：Maven（`mvn clean compile`、`mvn spring-boot:run`，本機跑在 **port 8081**）  
- **其他函式庫**：Lombok、Apache POI（Excel 匯出）、Cloudinary SDK（圖片雲端儲存）  
- **前端**：純 HTML \+ 原生 JS（LIFF 頁面），無框架  
- **套件名稱**：`com.petgrooming.pet_system`

## 二、部署與環境

- **GitHub**：`david810213-boop/mulage-pet-system`，開發分支 **`redesign`**  
- **正式環境**：Railway，專案名稱 `empowering-commitment`，內含 `MySQL` 服務 \+ `mulage-pet-system`（App）服務  
- **本機資料庫**：Docker 容器 `pet-mysql`，對外 **port 3307**（容器內部仍是 3306）  
  - ⚠️ 這台電腦另外還裝了一個**原生 MySQL（port 3306）**，兩者容易搞混，連線前務必先確認 port  
- **連線正式環境資料庫**：已安裝 Railway CLI，用 `railway link` → `railway connect MySQL`（不需要開 Public Networking，走 SSH tunnel）  
- **本機與正式環境資料庫完全獨立、不同步**——本機測試的資料不會出現在正式站，反之亦然  
- **環境變數**（`application.yml` 用 `${...}` 讀取，不寫死在檔案裡）： `DB_HOST/PORT/NAME/USERNAME/PASSWORD`、`JWT_SECRET`、`LINE_CHANNEL_ID`、`LINE_MESSAGING_CHANNEL_ACCESS_TOKEN`、`CLOUDINARY_CLOUD_NAME/API_KEY/API_SECRET`

## 三、雙軌身分認證架構

- **店家/員工**：帳號密碼登入網頁版，JWT 存在 Cookie，`LoginInterceptor` \+ `RoleInterceptor`（搭配 `@RequireRole` 註解）做權限控管  
- **會員（顧客）**：透過 LINE LIFF 頁面，`liff.getIDToken()` 驗證後換發 JWT  
- **⚠️ 重要**：新增任何 `/api/**` 端點，如果是要給「未登入狀態」呼叫的（例如 LINE 登入/綁定），**一定要手動加進 `WebConfig` 攔截器的白名單**，不然會被誤擋（踩過這個坑：`/api/line/bind` 一開始漏加）

## 四、LIFF 架構（✅ 已全數完成）

**確認結論：LINE 官方不支援「單一 LIFF ID \+ 路徑轉發」到不同頁面。** 在 LIFF ID 後面加路徑會被包進 `liff.state` 參數，瀏覽器只會停在 Endpoint URL 本身，不會真的導到接的那個路徑。

**正確做法：每個要在 LINE 裡開啟的獨立頁面，都要各自申請一個 LIFF App**，Endpoint URL 直接指向那個確切頁面，LIFF URL 使用時**不接任何路徑**。

**已申請的 LIFF App（9 個頁面全數獨立，之前記錄的「6 個頁面尚未處理」已補齊）：**

| 頁面 | 檔案 |
| :---- | :---- |
| 首頁 | index.html |
| 新客報到 | new-customer.html |
| 店員綁定LINE | bind-line.html |
| 編輯個人資料 | my-profile.html |
| 新增毛孩 | add-pet.html |
| 我的寵物 | my-pets.html |
| 預約 | booking.html |
| 我的預約 | my-appointments.html |
| 錢包 | wallet.html |

每個頁面現在都可以直接用自己的 LIFF URL 開啟，不再需要從首頁點進去才會正常運作。

**其他 LIFF 相關教訓：**

- `liff.login()` 一定要帶 `{ redirectUri: window.location.href }`，不然登入完成後可能跳轉錯誤（曾造成無限登入循環）  
- `lineUserId` 在資料庫是**唯一值**，同一支手機的 LINE 帳號只能綁一個系統帳號（顧客帳號或店員帳號擇一），測試時容易撞到自己先前用同一支 LINE 測試會員功能時自動建立的顧客帳號

## 五、資料庫踩過的坑（技術債提醒）

1. **MySQL 原生 ENUM 欄位**：Hibernate `ddl-auto=update` 不會自動幫已存在的 ENUM 欄位擴充允許值清單，Java enum 加新值後直接寫入會報 `Data truncated` 錯誤。已修正做法：**改用 VARCHAR 存 enum，不要依賴 MySQL 原生 ENUM**（`payment_method`、`source`、`status` 等欄位都已改成 VARCHAR）  
2. **新增 NOT NULL 欄位**：一律搭配 `columnDefinition = "... default ..."`，避免對已有資料的表格做 `ALTER TABLE` 時失敗  
3. **Thymeleaf inline JavaScript**（`/*[[${x}]]*/`）用 Jackson 序列化，如果把整個 JPA entity（尤其含 `LocalDateTime` 欄位）直接塞進去，容易序列化失敗導致整頁渲染中斷。**只塞畫面真正需要的欄位**（用 Map 或輕量 DTO，不要整個 entity）  
4. **Appointment / WalkInOrderItem 的品項名稱是「快照」**，不是即時關聯——服務項目改名不會影響歷史紀錄顯示的名稱，這是刻意設計（避免對帳時看到跟當初收據不符的名稱）  
5. **無 Flyway，靠 `DataInitializer`/`DataBackfillRunner` 當輕量 migration**：新欄位/新表由 Hibernate 自動建立，但既有資料的回填要自己寫程式補（例如 `bookable`、`applicablePetType`、`discountEligible` 這幾個後補欄位，都是靠「每次啟動都跑一次的校正迴圈」，不管資料庫現況為何，重複執行也不會有副作用）

## 六、服務項目與套餐化架構（本次更新重點）

**背景**：原本服務項目是「一個項目 \= 一個績效分類」的扁平結構（例如 `BATH_S` 洗澡小型犬）。改版後貓咪、狗狗的實際價目表要依「毛長/體型/服務深度」拆成幾十種組合，同一分類（例如 `BATH_CAT_S`）現在對應到很多不同價格的項目，於是發展出以下機制：

- **貓咪 28 項**（`CAT001`\~`CAT028`）：  
    
  - `CAT001`\~`CAT012`：初體驗價目表（12 項），給第一次上門的貓  
  - `CAT013`\~`CAT024`：單次服務價目表（12 項），給非首次的貓  
  - `CAT025`/`CAT026`：加購項目（牙齒清潔、天然草本泥浴），不計積分、不打折  
  - `CAT027`：貓咪基礎保養（低銷項目，`requiresExistingCustomer=true`，僅限老客戶）  
  - `CAT028`：長毛貓加購（搭配 CAT027），同樣僅限老客戶  
  - 舊版 `BATH_CS`/`BATH_CL`/`BLOW_CS`/`BLOW_CL` 已下架（`isDeleted=true`），保留歷史紀錄但選單不再顯示


- **狗狗 36 項**（`DOG001`\~`DOG036`）：6 個體重級距（小型5kg以下／中小型6-10kg／中型11-16kg／中大型17-22kg／大型23-27kg／特大型28-33kg）× 短毛/長毛 × 3 個服務等級（精緻洗／基礎定制調理／中階定制調理）＝ 36 種組合，實際定價已全部補齊。舊版 `BATH_S`/`BATH_L`/`BLOW_S`/`BLOW_L` 已下架。  
    
  - ⚠️「高階定制調理」（開放式報價）跟各種浮動加價（厚毛/長毛/特殊剪法/特殊情況）目前還沒建——系統還沒有「現場自訂金額加購」的機制，等這個功能做完才能把這批價目表項目建進去


- **套餐組成（`GroomingItemComponent`）**：一個套餐項目（例如「頂級專業定制洗護」）除了自己掛的主組成分類/積分外，可以再設定多個「副組成」（例如吹毛+基礎美容+AD），副組成 price 固定為 0（錢已經在主組成算過），純粹是讓「待補經手人」矩陣表單能正確拆出這次消費包含哪些積分項目，方便由不同美容師分開處理/分潤。  
    
- **適用物種（`GroomingItem.applicablePetType`，型別 `PetType`，DOG/CAT/null）**：LIFF 預約頁依所選毛孩物種自動篩選菜單——選貓只看到貓的項目，選狗只看到狗的項目，`null` 代表貓狗都適用（例如指甲修剪、擠肛門腺這類共用加購項目）。前端在 `booking.html` 用 `data-applicable-pet-type` 屬性整列隱藏不符合的項目。  
    
- **僅限既有客戶（`GroomingItem.requiresExistingCustomer`）**：像「貓咪基礎保養」這種低銷項目，店家規定不適用完全沒消費紀錄的新客寵物。前端整列隱藏，後端 `AppointmentService.isExistingCustomerPet()` / `WalkInOrderService.isExistingCustomerPet()` 也會再擋一次，判斷邏輯是「這隻寵物名下有沒有任何一筆已結帳紀錄」。  
    
- **積分覆寫（`GroomingItem.points`）**：同一個績效分類現在對應到很多不同價格的項目，不該全部套用分類的固定預設積分，所以開放建立項目時可以指定實際積分，不填才退回用分類預設值。  
    
- **體重定價門檻設定（`PricingSettings`，單例設定，後台路徑 `/payments/pricing-settings`）**：  
    
  - 貓咪體重加價門檻（基礎/中/高三段，附加價金額）  
  - 狗狗小型犬／大型犬體重切點，依毛長不同：**長毛 17kg 門檻、短毛/厚短毛 23kg 門檻**（可在後台調整）  
  - ⚠️ 這些都是「參考用」數字，系統不會自動運算加價，是店員核對/結帳時自行依畫面顯示的門檻手動選對應價格級距的項目

## 七、核心業務規則

- **折扣互斥**：會員儲值折扣（需求5）與貓咪90天回洗優惠（需求8）**只能擇一**（取較優惠者），不疊加；狗狗首次體驗9折跟會員儲值折扣同樣互斥擇一  
- **業績認列**：只有「結帳完成」（`Transaction`/`WalkInOrder` 的 `paid=true`）才算業績；**儲值本身是預收款，不算業績**  
- **公休判斷**：單一天公休（`ClosedDate`）+ 固定公休星期（`WeeklyClosureSetting`）兩套機制並存，符合任一條件即公休  
- **匯款流程**：選匯款先進入「待對帳」狀態，店家手動「確認收款」才轉為完成（預約結帳、現場開單皆適用）  
- **匯款收款帳戶**：結帳收款（`CHECKOUT`）與儲值金收款（`TOPUP`，大額專用）**是兩組獨立帳戶**  
- **庫存兩套機制**：零售商品（`RetailProduct`，賣給顧客，**結帳**扣庫存）vs 店用洗劑（`StoreSupply`，店家自用，**領用登記**扣庫存）  
- **服務項目代碼唯讀**：只有名稱/描述/金額/物種/是否限既有客戶可改，代碼建立後不可變更（型別層級保護，`UpdateGroomingItemRequest` DTO 沒有 itemCode 欄位）  
- **LIFF 菜單依物種篩選**：貓咪只看得到貓的服務項目，狗狗只看得到狗的，物種通用的加購項目（`applicablePetType=null`）兩邊都看得到

## 八、已完成的需求清單（原始22項全數完成 \+ 額外功能）

**基礎功能**：時段管理（含公休日）、積分計算與管理、現場開單、儲值金折扣、待補經手人、積分拆分、代客預約、乙方電子簽名檔、共用平板切換使用者

**本次開發週期新完成/大幅擴充的項目**：

- 需求6：財務報表（當日/當月業績拆分、儲值金vs非儲值金、零售+洗劑成本、Excel匯出）  
- 需求7：庫存管理（零售商品 \+ 店用洗劑，含低庫存LINE通知）  
- 需求8：貓咪90天回洗優惠（含現場開單會員擴充、折扣擇一邏輯）  
- 需求13：LINE自動推播（確認通知、當天預約自動確認、每日19:00提醒）  
- 需求14：服務項目名稱/描述/金額自主修改  
- 需求15：現場開單結帳畫面統一（含匯款支援、純零售訂單）  
- 需求16：公休日設定（單一天 \+ 固定星期）  
- 需求17/18：Cloudinary寵物照片、美容歷史相簿、現場開單防呆  
- 需求21：匯款帳戶QR Code、雙帳戶分離

**額外新增功能**（原始需求文件之外）：

- LIFF新客報到導引頁 \+ 圖文選單設計  
- 網頁版編輯個人資料（含店家代客搜尋填寫模式）、網頁版新增毛孩  
- 操作紀錄全面中文化  
- 店員綁定LINE帳號功能  
- **狗狗首次體驗 9 折**（`DogFirstVisitDiscountService`，跟貓咪回洗優惠是兩套獨立機制，判斷「這隻狗有沒有消費過」而非「有沒有回訪」）  
- **✅ LIFF 9 個頁面全數獨立成 LIFF App**（見第四節）  
- **✅ 貓咪 28 項 \+ 狗狗 36 項服務項目全面套餐化**，含適用物種篩選、僅限既有客戶限制、套餐副組成（見第六節）  
- **✅ 體重定價門檻設定**後台頁面（`/payments/pricing-settings`）

## 九、待處理事項

1. **零售商品成本回填**：`RetailProduct.unitCost` 欄位，既有商品資料預設是0，需要店家自行到後台補上正確進貨成本，財務報表的零售成本計算才會準確（目前沒有自動回填腳本）  
2. **假日/平日客單價分流**（尚未實作）：六日預約常爆滿，想在不得罪會員的前提下，讓高單價單次客盡量排假日、會員疏散到平日。已討論方向：假日折扣限平日才有 \+ 假日定價略高，是風險最低、跟現有折扣架構最契合的做法  
3. **狗狗「高階定制調理」與各種浮動加價**（新發現的待處理項目）：開放式報價（Xup）、厚毛/長毛/特殊剪法/特殊情況等加價，目前系統沒有「現場自訂金額加購」機制，等這個功能做完才能把價目表這部分建進去

## 十、給接手對話的操作提醒

- 每次修改都要交付**完整檔案**（不要只給片段），使用者曾多次因為套用不完整的 patch 導致編譯錯誤  
- 每次改動後主動做**括號/標籤配對覆查**（Python script 數 `{`/`}`、`<div>`/`</div>` 等），這個環境沒辦法連 Maven Central 實際編譯驗證  
- 交付前後端功能時，**務必先確認 DTO/entity 實際欄位存在**再寫程式碼（曾因為假設 `RetailProduct` 有 `unitCost` 欄位卻沒真的加，導致編譯失敗）  
- 每次 push 前提醒使用者跑敏感資訊檢查（`Select-String` 找 API key/secret 有沒有寫死）  
- 新增資料庫欄位/表格不用手動下 SQL（`ddl-auto=update` 自動處理），但 enum 型別欄位要優先用 VARCHAR  
- **新增到既有資料表的 NOT NULL 欄位，務必給資料庫層級 `columnDefinition` 預設值**，並在 `DataInitializer` 裡寫「每次啟動都校正一次」的回填/修正邏輯（不要只包在 `count()==0` 的區塊裡，正式站資料庫早就不是空的，那個區塊不會再執行）

